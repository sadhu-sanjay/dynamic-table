
export type SortConfig = {
    key: string;
    direction: string;
};

export type Record = {
    id: number;
    name: string;
    email: string;
    phone: string;
    address: string;
    description: string;
};

